version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - PIPELINE_GEMINI_MODEL=gemini-3.1-flash-lite
      - DASHBOARD_GEMINI_MODEL=gemini-3.5-flash
    volumes:
      - ./database.db:/app/database.db
    restart: always
